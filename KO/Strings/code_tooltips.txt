@code_tooltip_harvest = `harvest()`
드론 아래에 있는 entity를 수확합니다. 
수확할 수 없는 entity를 수확하려고 하면 파괴됩니다.

entity가 제거되면 `True`를, 그렇지 않으면 `False`를 반환합니다.

entity가 제거된 경우 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`harvest()`
@code_tooltip_can_harvest = `can_harvest()`
식물이 다 자랐는지 확인할 때 사용합니다.

드론 아래에 수확할 준비가 된 entity가 있으면 `True`를, 그렇지 않으면 `False`를 반환합니다.

`1` tick이 소요됩니다.

예시:
`if can_harvest():
    harvest()`
@code_tooltip_range = `range(start = 0, end, step = 1)`
`start`에서 시작해서 `step`만큼씩 숫자를 건너뛰며 `end`(는 포함되지 않음)에 도달하기 직전까지의 숫자 시퀀스를 생성합니다.

참고로 start는 기본적으로 `0`으로 설정되며, 인자가 하나만 주어지면 `end`에 할당됩니다. 이는 보통은 불가능한 방식입니다.
Python에서 `range`는 이런 이상한 동작을 허용하는 클래스 생성자입니다.

`1` tick이 소요됩니다.

예시:
`for i in range(10):
    print(i)

for i in range(2,6):
    print(i)

for i in range(10, 0, -1):
    print(i)`
@code_tooltip_plant = `plant(entity)` 
지정된 `entity`의 비용을 지불하고 드론 아래에 심습니다.
식물을 심을 비용이 부족하거나, 땅 종류가 맞지 않거나, 이미 다른 식물이 심어져 있으면 실패합니다.

성공하면 `True`를, 그렇지 않으면 `False`를 반환합니다.

성공한 경우 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`plant(Entities.Bush)`
@code_tooltip_move = `move(direction)`
드론을 지정된 `direction`으로 한 타일 이동시킵니다.
드론이 농장 가장자리 밖으로 이동하면 농장 반대편으로 돌아옵니다.

`East `  =  오른쪽
`West `  =  왼쪽
`North`  =  위쪽
`South`  =  아래쪽

드론이 움직였으면 `True`를, 그렇지 않으면 `False`를 반환합니다.

드론이 움직인 경우 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`move(North)`
@code_tooltip_can_move() = `can_move(direction)`
드론이 지정된 `direction`으로 이동할 수 있는지 확인합니다.

드론이 움직일 수 있으면 `True`를, 그렇지 않으면 `False`를 반환합니다.

`1` tick이 소요됩니다.

예시:
`if can_move(North):
    move(North)`
@code_tooltip_swap = `swap(direction)`
드론 아래의 entity를 지정된 `direction`에 있는 옆 entity와 서로 바꿉니다.
모든 entity에 작동하는 것은 아닙니다.
entity 중 하나(또는 둘 다)가 `None`인 경우에도 작동합니다.

성공하면 `True`를, 그렇지 않으면 `False`를 반환합니다.

성공 시 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`swap(North)`
@code_tooltip_till = `till()` 
드론 아래의 땅을 `Grounds.Soil`로 경작합니다. 이미 토양이라면 땅을 다시 `Grounds.Grassland`로 바꿉니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`till()`
@code_tooltip_get_pos_x = `get_pos_x()` 
드론의 현재 x 위치를 가져옵니다.
x 위치는 서쪽에서 `0`으로 시작하여 동쪽 방향으로 증가합니다.

드론의 현재 x 좌표를 나타내는 숫자를 반환합니다.

`1` tick이 소요됩니다.

예시:
`x, y = get_pos_x(), get_pos_y()`
@code_tooltip_get_pos_y = `get_pos_y()` 
드론의 현재 y 위치를 가져옵니다.
y 위치는 남쪽에서 `0`으로 시작하여 북쪽 방향으로 증가합니다.

드론의 현재 y 좌표를 나타내는 숫자를 반환합니다.

`1` tick이 소요됩니다.

예시:
`x, y = get_pos_x(), get_pos_y()`
@code_tooltip_get_world_size = `get_world_size()` 
농장의 현재 크기를 가져옵니다.

북쪽에서 남쪽 방향으로의 격자 한 변의 길이를 반환합니다.

`1` tick이 소요됩니다.

예시:
`for i in range(get_world_size()):
    move(North)`
@code_tooltip_get_entity_type = `get_entity_type()` 
드론 아래에 어떤 종류의 entity가 있는지 알아냅니다.

타일이 비어 있으면 `None`을 반환하고, 그렇지 않으면 드론 아래에 있는 entity의 종류를 반환합니다.

`1` tick이 소요됩니다.

예시:
`if get_entity_type() == Entities.Grass:
    harvest()`
@code_tooltip_get_ground_type = `get_ground_type()` 
드론 아래에 어떤 종류의 땅이 있는지 알아냅니다.

드론 아래에 있는 땅의 종류를 반환합니다.

`1` tick이 소요됩니다.

예시:
`if get_ground_type() != Grounds.Soil:
    till()`
@code_tooltip_get_time = `get_time()` 
현재 게임 시간을 가져옵니다.

게임 시작 후 경과 시간을 초 단위로 반환합니다.

`1` tick이 소요됩니다.

예시:
`start = get_time()

do_something()

time_passed = get_time() - start`
@code_tooltip_get_tick_count = `get_tick_count()`
수행된 tick의 수를 측정하는 데 사용됩니다.

실행 시작 후 수행된 tick의 수를 반환합니다.

`0` tick이 소요됩니다.

예시:
`do_something()

print(get_tick_count())`
@code_tooltip_use_item = `use_item(item, n=1)` 
지정된 `item`을 `n`번 사용하려고 시도합니다. `Items.Water`, `Items.Fertilizer`를 포함한 일부 아이템에만 사용할 수 있습니다.

아이템이 사용되었으면 `True`를, 그렇지 않으면 `False`를 반환합니다.

성공한 경우 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`use_item(Items.Fertilizer)`
@code_tooltip_get_water = `get_water()` 
드론 아래의 현재 물 수위를 가져옵니다.

드론 아래의 물 수위를 `0`과 `1` 사이의 숫자로 반환합니다.

`1` tick이 소요됩니다.

예시:
`if get_water() < 0.5:
    use_item(Items.Water)`
@code_tooltip_do_a_flip = `do_a_flip()` 
드론이 공중제비를 돕니다! 이 동작은 속도 업그레이드의 영향을 받지 않습니다.

`None`을 반환합니다.

실행하는 데 1초가 걸립니다.

예시:
`while True:
    do_a_flip()`
@code_tooltip_pet_the_piggy = `pet_the_piggy()` 
돼지를 쓰다듬어 줍니다! 이 동작은 속도 업그레이드의 영향을 받지 않습니다.

`None`을 반환합니다.

실행하는 데 1초가 걸립니다.

예시:
`while True:
    pet_the_piggy()`
@code_tooltip_print = `print(*args)` 
연기를 사용해 드론 위 공중에 모든 `args`를 출력합니다. 이 동작은 속도 업그레이드의 영향을 받지 않습니다.
여러 값을 한 번에 출력할 수 있습니다.

`None`을 반환합니다.

실행하는 데 1초가 걸립니다.

예시:
`print("ground:", get_ground_type())`
@code_tooltip_len = `len(collection)` 
list, set, dict 또는 tuple에 있는 요소의 개수를 가져옵니다.

`collection`의 길이를 반환합니다.

`1` tick이 소요됩니다.

예시:
`for i in range(len(list)):
    list[i] += 1`
@code_tooltip_list = `list(collection = None)`
새로운 list를 만듭니다. 
`collection`이 None이면, 빈 list를 만듭니다.
`collection`이 collection이면, 그 복사본을 만듭니다.

list를 반환합니다.

`1 + len(collection)` tick이 소요됩니다.

예시:
`new_list = list((1,2,3))`
@code_tooltip_dict = `dict(dictionary = None)`
새로운 dictionary를 만듭니다.
`dictionary`가 None이면, 빈 dictionary를 만듭니다.
`dictionary`가 dictionary이면, 그 복사본을 만듭니다.

dictionary를 반환합니다.

`1 + len(dictionary)` tick이 소요됩니다.

예시:
`new_dict = dict()`
@code_tooltip_set = `set(collection = None)`
새로운 set을 만듭니다.
`collection`이 None이면, 빈 set을 만듭니다.
`collection`이 값들의 collection이면, 그 값들을 담은 새로운 set을 만듭니다.

set을 반환합니다.

`1 + len(collection)` tick이 소요됩니다.

예시:
`new_set = set((1,2,3))`
@code_tooltip_str = `str(object)`

`object`의 문자열 표현을 반환합니다.

`1` tick이 소요됩니다.

예시:
`string = str(1000)`
@code_tooltip_set_execution_speed = `set_execution_speed(speed)`
프로그램 실행 속도를 제한하여 무슨 일이 일어나는지 더 잘 볼 수 있게 합니다.

`speed`가 `1`이면 속도 업그레이드가 없는 드론의 속도입니다.
`speed`가 `10`이면 코드가 `10`배 빠르게 실행되며, `9`번의 속도 업그레이드 후의 드론 속도에 해당합니다.
`speed`가 `0.5`이면 속도 업그레이드가 없는 속도의 절반으로 코드가 실행됩니다. 코드가 무엇을 하는지 볼 때 유용할 수 있습니다.

`speed`가 현재 실행 가능한 속도보다 빠르면 그냥 최대 속도로 실행됩니다.

`speed`가 `0`이거나 음수이면 속도는 다시 최대 속도로 변경됩니다.
이 효과는 실행이 멈추면 사라집니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`set_execution_speed(1)`
@code_tooltip_set_world_size = `set_world_size(size)`
농장 크기를 제한하여 무슨 일이 일어나는지 더 잘 볼 수 있게 합니다.
또한 농장을 비우고 드론 위치를 초기화합니다.
농장을 `size` x `size` 격자로 설정합니다.
가능한 가장 작은 `size`는 `3`입니다.
`size`가 `3`보다 작으면 격자는 다시 전체 크기로 돌아갑니다.
이 효과는 실행이 멈추면 사라집니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`set_world_size(5)`
@code_tooltip_num_items = `num_items(item)` 
현재 가지고 있는 `item`의 개수를 알아냅니다.

현재 인벤토리에 있는 `item`의 개수를 반환합니다.

`1` tick이 소요됩니다.

예시:
`if num_items(Items.Fertilizer) > 0:
    use_item(Items.Fertilizer)`
@code_tooltip_get_cost = `get_cost(thing)` 
`thing`의 비용을 가져옵니다.

`thing`이 entity이면 심는 비용을 가져옵니다.
`thing`이 해금 요소이면 해금 비용을 가져옵니다.

아이템을 key로, 숫자를 value로 하는 dictionary를 반환합니다. 각 아이템은 필요한 양에 매핑됩니다.
업그레이드 가능한 해금 요소가 이미 최대 레벨일 때 사용하면 `{}`를 반환합니다.

`1` tick이 소요됩니다.

예시:
`cost = get_cost(Unlocks.Carrots)
for item in cost:
    if num_items(item) < cost[item]:
        print("not enough items to unlock carrots")`
@code_tooltip_clear = `clear()` 
농장에서 모든 것을 제거하고, 드론을 위치 `(0,0)`으로 되돌리고, 모자를 밀짚모자로 다시 바꿉니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`clear()`
@code_tooltip_get_companion = `get_companion()` 
드론 아래에 있는 식물이 선호하는 동반 식물을 가져옵니다.

`(companion_type, (companion_x_position, companion_y_position))` 형태의 튜플을 반환합니다.

`1` tick이 소요됩니다.

예시:
`companion = get_companion()
if companion != None:
	print(companion)`
@code_tooltip_unlock = `unlock(unlock)` 
연구 트리에서 `unlock`에 해당하는 버튼을 클릭하는 것과 정확히 동일한 효과를 가집니다.

해금에 성공하면 `True`를, 그렇지 않으면 `False`를 반환합니다.

성공한 경우 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`unlock(Unlocks.Carrots)`
@code_tooltip_num_unlocked = `num_unlocked(thing)`
해금, entity, 땅 또는 아이템이 이미 해금되었는지 확인하는 데 사용됩니다.

`thing`이 업그레이드 가능하면 `1`에 `thing`이 업그레이드된 횟수를 더한 값을 반환합니다. 그렇지 않으면 `thing`이 해금되었으면 `1`을, 그렇지 않으면 `0`을 반환합니다.

`1` tick이 소요됩니다.

예시:
`plant(Entities.Bush)
n_substance = get_world_size() * num_unlocked(Unlocks.Mazes)
use_item(Items.Weird_Substance, n_substance)`
@code_tooltip_reset = `reset()` 
농장을 1x1 정사각형으로 초기화하고, 모든 자원을 제거하고, 대부분의 해금 요소를 잠급니다.
코드는 제거되지 않습니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`reset()`
@code_tooltip_measure = `measure(direction = None)` 
일부 entity에서 일부 값을 측정할 수 있습니다. 이 효과는 entity에 따라 다릅니다.

`direction`이 `None`이 아니면 주어진 방향의 이웃 entity를 측정합니다.

해바라기의 꽃잎 수를 반환합니다.
보물이나 사과의 다음 위치를 반환합니다.
선인장의 크기를 반환합니다.
호박에 대한 신비한 숫자를 반환합니다.
다른 모든 entity에 대해서는 `None`을 반환합니다.

`1` tick이 소요됩니다.

예시:
`num_petals = measure()`
@code_tooltip_leaderboard_run = `leaderboard_run(leaderboard, file_name, speedup)`
지정된 `file_name`을 시작점으로 사용하여 `leaderboard`에 대한 시간 측정 시뮬레이션을 시작합니다.
`speedup`은 시작 속도 배율을 설정합니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`leaderboard_run(Leaderboards.Fastest_Reset, "full_run", 256)`
@code_tooltip_simulate = `simulate(filename, sim_unlocks, sim_items, sim_globals, seed, speedup)`
지정된 `filename`을 시작점으로 사용하여 leaderboard용 시뮬레이션을 시작합니다.

`sim_unlocks`: 시작 해금 요소를 포함하는 시퀀스입니다.
`sim_items`: 아이템을 양에 매핑하는 dict입니다. 시뮬레이션은 이 아이템들로 시작합니다.
`sim_globals`: 변수 이름을 값에 매핑하는 dict입니다. 시뮬레이션은 이 변수들을 전역 scope에 가지고 시작합니다.
`seed`: 시뮬레이션의 랜덤 시드입니다. 양의 정수여야 합니다.
`speedup`: 시작 속도 배율입니다.

시뮬레이션을 실행하는 데 걸린 시간을 반환합니다.

`200` tick이 소요됩니다.

예시:
`filename = "f1"
sim_unlocks = Unlocks
sim_items = {Items.Carrot : 10000, Items.Hay : 50}
sim_globals = {"a" : 13}
seed = 0
speedup = 64

run_time = simulate(filename, sim_unlocks, sim_items, sim_globals, seed, speedup)`
@code_tooltip_spawn_drone = `spawn_drone(filename)`
`spawn_drone(function)` 명령을 실행한 드론과 같은 위치에 새 드론을 생성합니다. 그러면 새 드론은 지정된 함수를 실행하기 시작하고, 작업이 끝나면 자동으로 사라집니다.

새 드론의 핸들을 반환하며, 모든 드론이 이미 생성된 경우에는 `None`을 반환합니다.

드론이 생성된 경우 `200` tick, 그렇지 않은 경우 `1` tick이 소요됩니다.

예시:
`def harvest_column():
    for _ in range(get_world_size()):
        harvest()
        move(North)

while True:
    if spawn_drone(harvest_column):
        move(East)`

@code_tooltip_wait_for = `wait_for(drone)`
주어진 `drone`이 종료될 때까지 기다립니다.

`drone`이 실행하던 함수의 반환 값을 반환합니다.

기다리던 `drone`이 이미 작업을 마쳤다면 `1` tick이 소요됩니다.

예시:
`def get_entity_type_in_direction(dir):
    move(dir)
    return get_entity_type()

def zero_arg_wrapper():
    return get_entity_type_in_direction(North)
handle = spawn_drone(zero_arg_wrapper)
print(wait_for(handle))`

@code_tooltip_max_drones = `max_drones()`

농장에 가질 수 있는 최대 드론 수를 반환합니다.

`1` tick이 소요됩니다.

예시:
`while num_drones() < max_drones():
    spawn_drone("some_file_name")
    move(East)`

@code_tooltip_num_drones = `num_drones()`

현재 농장에 있는 드론의 수를 반환합니다.

`1` tick이 소요됩니다.

예시:
`while num_drones() < max_drones():
    spawn_drone("some_file_name")
    move(East)`

@code_tooltip_quick_print = `quick_print(*args)`
`print(*args)`와 똑같이 값을 출력하지만, 공중에 쓰기 위해 멈추지 않으므로 출력 페이지에서만 찾을 수 있습니다.

`None`을 반환합니다.

`0` tick이 소요됩니다.

예시:
`quick_print("hi mom")`
@code_tooltip_change_hat = `change_hat(hat)`
드론의 모자를 `hat`으로 변경합니다.

`None`을 반환합니다.

`200` tick이 소요됩니다.

예시:
`change_hat(Hats.Dinosaur_Hat)`

@code_tooltip_max = `max(*args)`
요소의 시퀀스 또는 여러 전달된 인자 중 최대값을 가져옵니다.
숫자와 문자열에 사용할 수 있습니다.

`max(a,b,c)`: `a`, `b`, `c`의 최대값을 반환합니다.
`max(sequence)`: 시퀀스의 모든 값 중 최대값을 반환합니다.

비교 횟수만큼 tick이 소요됩니다.

예시:
`max([3,6,34,16])`
@code_tooltip_min = `min(*args)`
요소의 시퀀스 또는 여러 전달된 인자 중 최소값을 가져옵니다.
숫자와 문자열에 사용할 수 있습니다.

`min(a,b,c)`: `a`, `b`, `c`의 최소값을 반환합니다.
`min(sequence)`: 시퀀스의 모든 값 중 최소값을 반환합니다.

비교 횟수만큼 tick이 소요됩니다.

예시:
`min([3,6,34,16])`
@code_tooltip_abs = `abs(number)`
숫자의 절댓값을 계산합니다.

`number`가 양수이면 `number`를, 그렇지 않으면 `-number`를 반환합니다.

`1` tick이 소요됩니다.

예시:
`abs(-69)`
@code_tooltip_random = `random()`
0(포함)과 1(미포함) 사이의 임의의 숫자를 샘플링합니다.

임의의 숫자를 반환합니다.

`1` tick이 소요됩니다.

예시:
`def random_elem(list):
	index = random() * len(list) // 1
	return list[index]`
@code_tooltip_append = `list.append(element)` 
`list`의 끝에 `element`를 추가합니다.

`None`을 반환합니다.

`1` tick이 소요됩니다.

예시:
`list = []
list.append(1)`
@code_tooltip_add = `set.add(element)` 
`set`에 `element`를 추가합니다.

`None`을 반환합니다.

`element size` tick이 소요됩니다.

예시:
`set = {0}
set.add(1)`
@code_tooltip_remove = `collection.remove(element)` 
`collection`에서 `element`의 첫 번째 발생을 제거합니다.

`None`을 반환합니다.

set에서는 `element size` tick, list에서는 비교 횟수 + 이동 횟수만큼 tick이 소요됩니다.

예시:
`list = [True, False, None]
list.remove(False)`
@code_tooltip_pop = `collection.pop()` 
list에서 마지막 요소를 제거하거나 dictionary에서 지정된 요소를 제거합니다.
`list.pop(i)`는 `list`에서 인덱스 `i`의 요소를 제거합니다.

제거된 요소를 반환합니다.

dictionary에서는 `key size` tick, list에서는 `len(list) - i + 1` tick이 소요됩니다.

예시:
`list = [True, False, None]
list.pop(0)`
@code_tooltip_insert = `list.insert(i, element)` 
`list`의 인덱스 `i`에 `element`를 삽입합니다.

`None`을 반환합니다.

`len(list) - i + 1` tick이 소요됩니다.

예시:
`list = [1,2]
list.insert(0, 0)`
@code_tooltip_Items = 인벤토리에 있을 수 있는 모든 아이템을 포함합니다. `for` 루프로 반복할 수 있습니다.
@code_tooltip_Entities = 모든 식물 종류를 포함합니다. `for` 루프로 반복할 수 있습니다.
@code_tooltip_Grounds = 가능한 모든 땅 종류를 포함합니다. `for` 루프로 반복할 수 있습니다.
@code_tooltip_Unlocks = 연구 메뉴의 모든 해금 및 업그레이드를 포함합니다. `for` 루프로 반복할 수 있습니다.
@code_tooltip_Hats = 모든 모자 종류를 포함합니다. `for` 루프로 반복할 수 있습니다.
@code_tooltip_Leaderboards = 모든 leaderboard 카테고리를 포함합니다. `for` 루프로 반복할 수 있습니다.
@code_tooltip_for = 시퀀스의 모든 요소를 반복하는 루프입니다. 일부 프로그래밍 언어에서는 "foreach" 루프라고 부릅니다.
@code_tooltip_while = 조건이 거짓이 될 때까지 반복합니다.
@code_tooltip_def = 함수를 정의합니다.
@code_tooltip_True = 항상 참인 불리언 값입니다.
@code_tooltip_False = 항상 거짓인 불리언 값입니다.
@code_tooltip_if = 조건이 `True`이면 코드를 실행합니다.
@code_tooltip_else = 이전 `if` 조건이 `False`인 경우 코드를 실행합니다.
@code_tooltip_elif = 다음과 같은 작업을 수행합니다:
`else:
    if condition:`
@code_tooltip_None = 값이 없음을 나타내는 값입니다.
@code_tooltip_continue = 즉시 다음 루프 반복으로 계속합니다. 중첩 루프가 있는 경우 항상 가장 안쪽 루프에 영향을 줍니다.
@code_tooltip_break = 루프를 빠져나와 루프 다음의 문장을 계속 실행합니다. 중첩 루프가 있는 경우 항상 가장 안쪽 루프에 영향을 줍니다.
@code_tooltip_North = 화면의 위쪽 방향입니다. 화면을 돌리지 않는 한 말이죠.
@code_tooltip_East = 화면의 오른쪽 방향입니다. 화면을 돌리지 않는 한 말이죠.
@code_tooltip_South = 화면의 아래쪽 방향입니다. 화면을 돌리지 않는 한 말이죠.
@code_tooltip_West = 화면의 왼쪽 방향입니다. 화면을 돌리지 않는 한 말이죠.
@code_tooltip_not = `not True`는 `False`이고 `not False`는 `True`입니다.
@code_tooltip_and = 첫 번째 피연산자를 평가합니다. 만약 거짓(falsy)(`False`, `0`, 빈 collection)이면, 즉시 그 값을 반환합니다(평가 단축). 그렇지 않으면 두 번째 피연산자를 평가하고 반환합니다.
@code_tooltip_or = 첫 번째 피연산자를 평가합니다. 만약 참(truthy)(`False`, `0`, 빈 collection 이외의 모든 것)이면, 즉시 그 값을 반환합니다(평가 단축). 그렇지 않으면 두 번째 피연산자를 평가하고 반환합니다.
@code_tooltip_return = 함수에서 값을 반환하는 데 사용됩니다.
@code_tooltip_pass = 아무것도 하지 않습니다. 빈 코드 블록은 허용되지 않으므로 유용할 수 있습니다.