# 註解
註解是程式碼中在執行期間會被忽略的部分。
可以使用 `#` 新增註解。 同一行位於 `#` 之後的任何內容都是註解，會被忽略。

```
# 這是一條註解
```

註解可以用於在程式碼中新增筆記，以及在不刪除部分程式碼的情況下暫時停用它們。

位於函式定義前一行的任何註解，都將成為滑鼠懸停在函式名稱上時彈出的資訊的一部分。
```
# 這個函式不會執行任何動作
def f():
    pass
```