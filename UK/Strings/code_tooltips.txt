@code_tooltip_harvest = `harvest()`
Збирає об'єкт під дроном. 
Якщо ви збираєте об'єкт, який неможливо зібрати, його буде знищено.

повертає `True`, якщо об'єкт було видалено, і `False` у іншому випадку.

виконання займає `200` тіків, якщо об'єкт видалено, і `1` тік у іншому випадку.

приклад:
`harvest()`
@code_tooltip_can_harvest = `can_harvest()`
Використовується, щоб дізнатися, чи повністю виросла рослина.

повертає `True`, якщо під дроном є об'єкт, готовий до збору, і `False` у іншому випадку.

виконання займає `1` тік.

приклад:
`if can_harvest():
    harvest()`
@code_tooltip_range = `range(start = 0, end, step = 1)`
Генерує послідовність чисел, починаючи з `start`, закінчуючи безпосередньо перед `end` (отже, `end` не включається) з кроком — `step`.

Зауважте, що start за замовчуванням дорівнює `0`, і якщо вказано лише один аргумент, він буде прив'язаний до `end`. Зазвичай це неможливо.
У Python `range` — це конструктор класу, який дозволяє таку поведінку.

виконання займає `1` тік.

приклад:
`for i in range(10):
    print(i)

for i in range(2,6):
    print(i)

for i in range(10, 0, -1):
    print(i)`
@code_tooltip_plant = `plant(entity)` 
Витрачає вартість вказаного об'єкта (`entity`) і висаджує його під дроном.
Команда не спрацює, якщо у вас недостатньо ресурсів, неправильний тип ґрунту або там уже щось посаджено.

повертає `True`, якщо висадка вдалася, і `False` у іншому випадку.

виконання займає `200` тіків у разі успіху, і `1` тік у іншому випадку.

приклад:
`plant(Entities.Bush)`
@code_tooltip_move = `move(direction)`
Переміщує дрона у вказаному напрямку (`direction`) на одну клітинку.
Якщо дрон виходить за межі ферми, він з'являється з протилежного боку.

`East `  =  праворуч
`West `  =  ліворуч
`North`  =  вгору
`South`  =  вниз

повертає `True`, якщо дрон перемістився, і `False` у іншому випадку.

виконання займає `200` тіків, якщо дрон перемістився, і `1` тік у іншому випадку.

приклад:
`move(North)`
@code_tooltip_can_move = `can_move(direction)`
Перевіряє, чи може дрон рухатися у вказаному напрямку.

повертає `True`, якщо дрон може рухатися, і `False` у іншому випадку.

виконання займає `1` тік.

приклад:
`if can_move(North):
    move(North)`
@code_tooltip_swap = `swap(direction)`
Міняє місцями об'єкт під дроном з об'єктом, що знаходиться поруч у вказаному напрямку.
Працює не з усіма об'єктами.
Також працює, якщо один (або обидва) об'єкти мають значення `None`.

повертає `True`, якщо обмін вдався, і `False` у іншому випадку.

виконання займає `200` тіків у разі успіху, і `1` тік у іншому випадку.

приклад:
`swap(North)`
@code_tooltip_till = `till()` 
Оре землю під дроном, перетворюючи її на ґрунт (`Grounds.Soil`). Якщо це вже ґрунт, він перетворить його назад на пасовище (`Grounds.Grassland`).

повертає `None`

виконання займає `200` тіків.

приклад:
`till()`
@code_tooltip_get_pos_x = `get_pos_x()` 
Отримує поточну позицію дрона по осі x.
Координата x починається з `0` на заході і збільшується у східному напрямку.

повертає число, що представляє поточну координату x дрона.

виконання займає `1` тік.

приклад:
`x, y = get_pos_x(), get_pos_y()`
@code_tooltip_get_pos_y = `get_pos_y()` 
Отримує поточну позицію дрона по осі y.
Координата y починається з `0` на півдні і збільшується у північному напрямку.

повертає число, що представляє поточну координату y дрона.

виконання займає `1` тік.

приклад:
`x, y = get_pos_x(), get_pos_y()`
@code_tooltip_get_world_size = `get_world_size()` 
Отримує поточний розмір ферми.

повертає довжину сторони поля у напрямку з півночі на південь.

виконання займає `1` тік.

приклад:
`for i in range(get_world_size()):
    move(North)`
@code_tooltip_get_entity_type = `get_entity_type()` 
Визначає, який тип об'єкта знаходиться під дроном.

повертає `None`, якщо клітинка порожня, інакше повертає тип об'єкта.

виконання займає `1` тік.

приклад:
`if get_entity_type() == Entities.Grass:
    harvest()`
@code_tooltip_get_ground_type = `get_ground_type()` 
Визначає, який тип землі під дроном.

повертає тип землі під дроном.

виконання займає `1` тік.

приклад:
`if get_ground_type() != Grounds.Soil:
    till()`
@code_tooltip_get_time = `get_time()` 
Отримує поточний ігровий час.

повертає час у секундах з початку гри.

виконання займає `1` тік.

приклад:
`start = get_time()

do_something()

time_passed = get_time() - start`
@code_tooltip_get_tick_count = `get_tick_count()`
Використовується для вимірювання кількості виконаних тіків.

повертає кількість тіків, виконаних з моменту початку виконання.

виконання займає `0` тіків.

приклад:
`do_something()

print(get_tick_count())`
@code_tooltip_use_item = `use_item(item, n=1)` 
Намагається використати вказаний предмет (`item`) `n` разів. Можна використовувати лише з деякими предметами, зокрема `Items.Water`, `Items.Fertilizer`.

повертає `True`, якщо предмет було використано, і `False` у іншому випадку.

виконання займає `200` тіків у разі успіху, і `1` тік у іншому випадку.

приклад:
`use_item(Items.Fertilizer)`
@code_tooltip_get_water = `get_water()` 
Отримує поточний рівень води під дроном.

повертає рівень води як число від `0` до `1`.

виконання займає `1` тік.

приклад:
`if get_water() < 0.5:
    use_item(Items.Water)`
@code_tooltip_do_a_flip = `do_a_flip()` 
Змушує дрона зробити сальто! На цю дію не впливають покращення швидкості.

повертає `None`

виконання займає 1с.

приклад:
`while True:
    do_a_flip()`
@code_tooltip_pet_the_piggy = `pet_the_piggy()` 
Погладити поросятко! На цю дію не впливають покращення швидкості.

повертає `None`

виконання займає 1с.

приклад:
`while True:
    pet_the_piggy()`
@code_tooltip_print = `print(*args)` 
Друкує усі аргументи (`args`) у повітрі над дроном за допомогою диму. На цю дію не впливають покращення швидкості.
Можна виводити кілька значень одночасно.

повертає `None`

виконання займає 1с.

приклад:
`print("земля:", get_ground_type())`
@code_tooltip_len = `len(collection)` 
Отримує кількість елементів у списку (`list`), множині(`set`), словнику (`dict`) або кортежі (`tuple`).

повертає довжину колекції (`collection`).

виконання займає `1` тік.

приклад:
`for i in range(len(list)):
    list[i] += 1`
@code_tooltip_list = `list(collection = None)`
Створює новий список. 
Якщо `collection` дорівнює `None`, створюється порожній список.
Якщо `collection` є послідовністю, створюється новий список з елементів цієї послідовності.

повертає список.

виконання займає `1 + len(collection)` тіків.

приклад:
`new_list = list((1,2,3))`
@code_tooltip_dict = `dict(dictionary = None)`
Створює новий словник.
Якщо `dictionary` дорівнює `None`, створюється порожній словник.
Якщо `dictionary` є словником, створюється його копія.

повертає словник.

виконання займає `1 + len(dictionary)` тіків.

приклад:
`new_dict = dict()`
@code_tooltip_set = `set(collection = None)`
Створює нову множину.
Якщо `collection` дорівнює `None`, створюється порожня множина.
Якщо `collection` є набором значень, створюється нова множина з цими значеннями.

повертає множину.

виконання займає `1 + len(collection)` тіків.

приклад:
`new_set = set((1,2,3))`
@code_tooltip_str = `str(object)`

повертає рядкове представлення об'єкта (`object`).

виконання займає `1` тік.

приклад:
`string = str(1000)`
@code_tooltip_set_execution_speed = `set_execution_speed(speed)`
Обмежує швидкість, з якою виконується програма, щоб краще бачити, що відбувається.

`speed` рівна `1` — це швидкість, яку дрон має без жодних покращень.
`speed` рівна `8` змушує код виконуватися у `8` разів швидше (відповідає швидкості дрона після 3 покращень швидкості).
`speed` рівна `0.5` змушує код виконуватися вдвічі повільніше за базову швидкість. Це корисно для відладки коду.

Якщо `speed` вища, ніж дрон може зараз розвинути, він просто працюватиме на максимальній швидкості.

Якщо `speed` дорівнює `0` або є від'ємною, швидкість повертається до максимальної.
Ефект також припиняється, коли виконання коду зупиняється.

повертає `None`

виконання займає `200` тіків.

приклад:
`set_execution_speed(1)`
@code_tooltip_set_world_size = `set_world_size(size)`
Обмежує розмір ферми, щоб краще бачити, що відбувається.
Також очищує ферму та скидає позицію дрона.
Створює ферму розміром `size` x `size`.
Мінімально можливий розмір — `3`.
Розмір менше `3` поверне ферму до її повного розблокованого розміру.
Ефект припиняється, коли виконання коду зупиняється.

повертає `None`

виконання займає `200` тіків.

приклад:
`set_world_size(5)`
@code_tooltip_num_items = `num_items(item)` 
Дозволяє дізнатися, скільки одиниць певного предмета (`item`) ви зараз маєте.

повертає кількість предметів (`item`) в інвентарі.

виконання займає `1` тік.

приклад:
`if num_items(Items.Fertilizer) > 0:
    use_item(Items.Fertilizer)`
@code_tooltip_get_cost = `get_cost(thing)` 
Отримує вартість об'єкта (`thing`).

Якщо `thing` — це рослина, повертає вартість її посадки.
Якщо `thing` — це розблокування, повертає вартість його дослідження.

повертає словник, де ключі — це предмети, а значення — їхня кількість.
повертає `{}`, якщо покращення вже досягло максимального рівня.

виконання займає `1` тік.

приклад:
`cost = get_cost(Unlocks.Carrots)
for item in cost:
    if num_items(item) < cost[item]:
        print("недостатньо ресурсів для розблокування моркви")`
@code_tooltip_clear = `clear()` 
Видаляє все з ферми, повертає дрона в позицію `(0,0)` і змінює капелюх на солом'яний.

повертає `None`

виконання займає `200` тіків.

приклад:
`clear()`
@code_tooltip_get_companion = `get_companion()` 
Отримує бажаного "сусіда" (companion) для рослини під дроном.

повертає кортеж вигляду `(тип_сусіда, (x_позиція, y_позиція))`.

виконання займає `1` тік.

приклад:
`companion = get_companion()
if companion != None:
    plant_type, (x, y) = companion
    print("Сусід:", plant_type, "у", x, ",", y)`
@code_tooltip_unlock = `unlock(unlock)` 
Має точно такий самий ефект, як натискання кнопки розблокування в дереві досліджень.

повертає `True`, якщо розблокування вдалося, і `False` у іншому випадку.

виконання займає `200` тіків у разі успіху, і `1` тік у іншому випадку.

приклад:
`unlock(Unlocks.Carrots)`
@code_tooltip_num_unlocked = `num_unlocked(thing)`
Використовується для перевірки, чи розблоковано покращення, рослину, землю, предмет або капелюх.

повертає `1` плюс кількість разів, на яку `thing` було покращено (якщо це покращуваний об'єкт). В іншому випадку повертає `1`, якщо відкрито, і `0`, якщо ні.

виконання займає `1` тік.

приклад:
`plant(Entities.Bush)
n_substance = get_world_size() * num_unlocked(Unlocks.Mazes)
use_item(Items.Weird_Substance, n_substance)`
@code_tooltip_reset = `reset()` 
Скидає ферму назад до квадрата 1x1, видаляє всі ресурси та блокує більшість розблокувань.
Це не видаляє ваш код.

повертає `None`

виконання займає `200` тіків.

приклад:
`reset()`
@code_tooltip_measure = `measure(direction = None)` 
Вимірює певні значення деяких об'єктів. Ефект залежить від типу об'єкта.

Якщо вказано `direction`, вимірює сусідній об'єкт у цьому напрямку.

повертає кількість пелюсток соняшника.
повертає наступну позицію для скарбу або яблука.
повертає розмір кактуса.
повертає таємниче число для гарбуза.
повертає `None` для всіх інших.

виконання займає `1` тік.

приклад:
`num_petals = measure()`
@code_tooltip_leaderboard_run = `leaderboard_run(leaderboard, file_name, speedup)`
Запускає симуляцію на час для таблиці лідерів (`leaderboard`), використовуючи вказаний файл (`file_name`) як точку старту.
`speedup` встановлює початкове прискорення.

повертає `None`

виконання займає `200` тіків.

приклад:
`leaderboard_run(Leaderboards.Fastest_Reset, "full_run", 256)`
@code_tooltip_simulate = `simulate(filename, sim_unlocks, sim_items, sim_globals, seed, speedup)`
Запускає симуляцію, використовуючи вказаний файл як точку старту.

`sim_unlocks`: Набір початкових розблокувань.
`sim_items`: Словник (dict) предметів та їх кількості. Симуляція починається з заданою кількістю цих предметів.
`sim_globals`: Словник (dict) назв змінних та їх значень. Симуляція починається з цими змінними у глобальній області видимості.
`seed`: Випадковий seed симуляції. Має бути додатнім цілим числом.
`speedup`: Початкове прискорення.

повертає час, який знадобився для виконання симуляції.

виконання займає `200` тіків.

приклад:
`filename = "f1"
sim_unlocks = Unlocks
sim_items = {Items.Carrot : 10000, Items.Hay : 50}
sim_globals = {"a" : 13}
seed = 0
speedup = 64

run_time = simulate(filename, sim_unlocks, sim_items, sim_globals, seed, speedup)`
@code_tooltip_spawn_drone = `spawn_drone(function)`
Створює нового дрона в тій же позиції, що й дрон, який запустив команду `spawn_drone(function)`. Новий дрон починає виконувати вказану функцію. Після завершення він автоматично зникне.

повертає ідентифікатор (handle) нового дрона або `None`, якщо всі дрони вже створені.

виконання займає `200` тіків, якщо дрон був створений, і `1` в іншому випадку.

приклад:
`def harvest_column():
    for _ in range(get_world_size()):
        harvest()
        move(North)

while True:
    if spawn_drone(harvest_column):
        move(East)`

@code_tooltip_wait_for = `wait_for(drone)`
Чекає, поки вказаний дрон (`drone`) завершить роботу.

повертає значення, яке повернула функція, що виконувалася цим дроном.

виконання займає `1` тік, якщо очікуваний дрон уже закінчив роботу.

приклад:
`def get_entity_type_in_direction(dir):
    move(dir)
    return get_entity_type()

def zero_arg_wrapper():
    return get_entity_type_in_direction(North)
handle = spawn_drone(zero_arg_wrapper)
print(wait_for(handle))`

@code_tooltip_has_finished = `has_finished(drone)`
Перевіряє, чи закінчив роботу вказаний дрон.

повертає `True`, якщо дрон закінчив роботу, і `False` в іншому випадку.

виконання займає `1` тік.

приклад:
`drone = spawn_drone(function)
while not has_finished(drone):
    do_something_else()
result = wait_for(drone)`

@code_tooltip_max_drones = `max_drones()`

повертає максимальну кількість дронів, яку ви можете мати на фермі.

виконання займає `1` тік.

приклад:
`while num_drones() < max_drones():
    spawn_drone("some_file_name")
    move(East)`

@code_tooltip_num_drones = `num_drones()`

повертає кількість дронів, які зараз знаходяться на фермі.

виконання займає `1` тік.

приклад:
`while num_drones() < max_drones():
    spawn_drone("some_file_name")
    move(East)`

@code_tooltip_quick_print = `quick_print(*args)`
Друкує значення так само, як `print(*args)`, але не зупиняється, щоб малювати його в повітрі, тому його можна знайти лише на сторінці виводу (output).

повертає `None`

виконання займає `0` тіків.

приклад:
`quick_print("привіт, мамо")`
@code_tooltip_change_hat = `change_hat(hat)`
Змінює капелюх дрона на `hat`.

повертає `None`

виконання займає `200` тіків.

приклад:
`change_hat(Hats.Dinosaur_Hat)`

@code_tooltip_max = `max(*args)`
Знаходить максимум із послідовності елементів або кількох переданих аргументів.
Можна використовувати для чисел (numbers) і рядків (strings).

`max(a,b,c)`: Повертає максимум серед `a`, `b` та `c`.
`max(sequence)`: Повертає максимум серед усіх значень у послідовності.

виконання займає кількість тіків, що дорівнює кількості порівнянь.

приклад:
`max([3,6,34,16])`
@code_tooltip_min = `min(*args)`
Знаходить мінімум із послідовності елементів або кількох переданих аргументів.
Можна використовувати для чисел (numbers) і рядків (strings).

`min(a,b,c)`: Повертає мінімум серед `a`, `b` та `c`.
`min(sequence)`: Повертає мінімум серед усіх значень у послідовності.

виконання займає кількість тіків, що дорівнює кількості порівнянь.

приклад:
`min([3,6,34,16])`
@code_tooltip_abs = `abs(number)`
Обчислює абсолютне значення (модуль) числа.

повертає `number`, якщо число позитивне, і `-number` в іншому випадку.

виконання займає 1 тік.

приклад:
`abs(-69)`
@code_tooltip_random = `random()`
Генерує випадкове число від 0 (включно) до 1 (не включно).

повертає випадкове число.

виконання займає `1` тік.

приклад:
`def random_elem(list):
	index = random() * len(list) // 1
	return list[index]`
@code_tooltip_append = `list.append(element)`
Додає елемент (`element`) у кінець списку (`list`).

повертає `None`

виконання займає `1` тік.

приклад:
`list = []
list.append(1)`
@code_tooltip_add = `set.add(element)`
Додає елемент (`element`) у множину (`set`).

повертає `None`

виконання займає тіки відповідно до розміру елемента.

приклад:
`set = {0}
set.add(1)`
@code_tooltip_remove = `collection.remove(element)`
Видаляє перше входження елемента (`element`) з колекції (`collection`).

повертає `None`

виконання для множині (set) займає тіки відповідно до розміру елемента, для списку (list) — кількість порівнянь + кількість зсувів.

приклад:
`list = [True, False, None]
list.remove(False)`
@code_tooltip_pop = `collection.pop()`
Видаляє останній елемент зі списку (list) або вказаний елемент зі словника (dict).
`list.pop(i)` видаляє елемент за індексом `i` зі списку.

повертає видалений елемент.

виконання на словнику займає тіки відповідно до розміру ключа, на списку — `len(list) - i + 1` тіків.

приклад:
`list = [True, False, None]
list.pop(0)`
@code_tooltip_insert = `list.insert(i, element)`
Вставляє елемент (`element`) у список за індексом `i`.

повертає `None`

виконання займає `len(list) - i + 1` тіків.

приклад:
`list = [1,2]
list.insert(0, 0)`
@code_tooltip_Items = Містить усі предмети, які можуть бути в інвентарі. Можна перебирати циклом `for`.
@code_tooltip_Entities = Містить усі типи рослин/об'єктів. Можна перебирати циклом `for`.
@code_tooltip_Grounds = Містить усі можливі типи землі. Можна перебирати циклом `for`.
@code_tooltip_Unlocks = Містить усі розблокування та покращення в меню досліджень. Можна перебирати циклом `for`.
@code_tooltip_Hats = Містить усі типи капелюхів. Можна перебирати циклом `for`.
@code_tooltip_Leaderboards = Містить усі категорії таблиць лідерів. Можна перебирати циклом `for`.
@code_tooltip_for = Цикл, який перебирає всі елементи послідовності. Деякі мови програмування називають це циклом "foreach".
@code_tooltip_while = Цикл, що триває, поки умова є істинною.
@code_tooltip_def = Визначає власну функцію.
@code_tooltip_True = Логічне значення, яке завжди істинне.
@code_tooltip_False = Логічне значення, яке завжди хибне.
@code_tooltip_if = Виконує код, якщо умова дорівнює `True`.
@code_tooltip_else = Виконує код, якщо попередня умова `if` була хибною (`False`).
@code_tooltip_elif = Робить те саме, що й:
`else:
    if condition:`
@code_tooltip_None = Значення, що вказує на відсутність будь-якого значення.
@code_tooltip_continue = Негайно перейти до наступної ітерації циклу. Якщо є вкладені цикли, це завжди впливає на внутрішніший цикл у якому безпосередньо розміщений `continue`
@code_tooltip_break = Вийти з циклу та продовжити виконання інструкцій після нього. Якщо є вкладені цикли, це завжди впливає на внутрішніший цикл у якому безпосередньо розміщений `break`.
@code_tooltip_North = Напрямок "вгору" на екрані. Якщо ви не перевернули свій екран.
@code_tooltip_East = Напрямок "праворуч" на екрані. Якщо ви не перевернули свій екран.
@code_tooltip_South = Напрямок "вниз" на екрані. Якщо ви не перевернули свій екран.
@code_tooltip_West = Напрямок "ліворуч" на екрані. Якщо ви не перевернули свій екран.
@code_tooltip_not = `not True` — це `False`, а `not False` — це `True`.
@code_tooltip_and = Обчислює перший операнд. Якщо він хибний (`False`, `0` або порожня колекція), він повертає це значення негайно (коротке замикання обчислення), інакше обчислює та повертає другий операнд.
@code_tooltip_or = Обчислює перший операнд. Якщо він істинний (будь-що, крім `False`, `0` або порожньої колекції), він повертає це значення негайно (коротке замикання обчислення), інакше обчислює та повертає другий операнд.
@code_tooltip_return = Використовується для повернення значення з функції.
@code_tooltip_pass = Нічого не робить. Може бути корисним, оскільки порожні блоки коду 